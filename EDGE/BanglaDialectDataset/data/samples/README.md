Place optional tiny sample PDFs / images here for smoke tests.
Production scanned books go in ../pdfs/.

`paddle_smoke.png` is a generated English test image used to verify `--engine paddle`
(PaddleOCR-VL) without needing a full book PDF.
