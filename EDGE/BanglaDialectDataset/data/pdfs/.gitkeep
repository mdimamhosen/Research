# Keep this folder in git. Drop scanned book PDFs here (*.pdf).
# PDFs themselves are gitignored — only this placeholder is committed.
# Run: .\.venv\Scripts\python.exe cli.py -i data/pdfs -o data/txt --skip-existing
