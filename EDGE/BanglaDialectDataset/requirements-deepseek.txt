# Optional: DeepSeek-OCR open VLM (GPU strongly recommended, ~8GB+ VRAM)
#
#   pip install -r requirements-deepseek.txt
#
# First run downloads weights from Hugging Face (multi-GB).
# Then:
#   python cli.py -i data/pdfs -o data/txt --engine deepseek --page-start 1 --page-end 1

torch>=2.2.0
transformers>=4.46.0,<4.50.0
tokenizers>=0.20.0
einops
addict
easydict
Pillow>=10.0.0
