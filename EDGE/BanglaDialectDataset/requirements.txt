# CLI core + PaddleOCR-VL (only engine)
pymupdf>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0
tqdm>=4.66.0
numpy>=1.26.0

# PaddleOCR (install paddlepaddle wheel FIRST — see README / Dockerfile)
paddleocr>=3.0.0
paddlex[ocr]>=3.7.0
